version: "3.9"
services:
  app:
    build: .
    image: financial-news-summariser:latest
    container_name: finnews
    ports:
      - "8501:8501"  # Streamlit
    volumes:
      - ./:/app
    command: streamlit run app/dashboard.py --server.port 8501 --server.headless true
